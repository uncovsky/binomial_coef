#pragma once

#include <math.h>
#include <string>
#include <vector>
#include <iomanip>
#include <iostream>
#include <algorithm>

std::string add_nums(std::string num1, std::string num2);
std::string huge_fact(int n, int k);
std::string multiply_nums(std::string num1, std::string num2);
std::string divide_num(std::string num, int divisor);
std::string binomial_coefficient(int n, int k);
